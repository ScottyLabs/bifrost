{
  "name": "infra",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": [],
  "targets": {
    "diff": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cdk diff"
      },
      "cache": false
    },
    "synth": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cdk synth"
      }
    }
  }
}
